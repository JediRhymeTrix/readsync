{
    "name": "readsync-e2e",
    "private": true,
    "version": "0.6.0",
    "description": "Playwright end-to-end tests for the ReadSync admin UI.",
    "scripts": {
        "test": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.45.0"
    }
}
